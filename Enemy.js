"use strict";

class Enemy extends Actor {
  constructor(ctx, id, img, x, y, width, height, hp, movSpeed) {
      super(ctx, id, img, x, y, width, height, hp, movSpeed);
  }

  var toRemove = false;

  draw(ctx) {

  }

  update() {
      
  }
}
